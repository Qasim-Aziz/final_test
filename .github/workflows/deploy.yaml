name: Deployment Workflow
on:
  push:
    branches:
      - master
jobs:
  job_one:
    name: Deploy
    runs-on: ubuntu-latest
    steps:
    - name: testing Deployment
      uses: appleboy/ssh-action@master
      with:
        host: 34.121.38.48.10.20
        username: qasim
        key: ${{ secrets.OMG_SECRET }}
        port: 22
        script: |
          ls 
          git clone https://github.com/qasim683/final_test.git
          cd final_test
          docker-compose up















   
          